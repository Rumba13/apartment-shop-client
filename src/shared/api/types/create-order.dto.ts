export type CreateOrderDto = {
    apartmentId: string,
    fromDate: string,
    toDate: string,
    guestsQuantity: number,
    phoneNumber: string,
    username: string,
    comment: string
}