import "./styles.scss";

type PropsType = {}

export const SearchButton = ({}: PropsType) => {
    return (
        <div className="search-button">
            SearchButton
        </div>
    )
}